name: Buf Push

on:
  push:
    branches: [main]
    paths:
      - 'proto/**'

jobs:
  push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: bufbuild/buf-action@v1
        with:
          push: true
          token: ${{ secrets.BUF_TOKEN }}
